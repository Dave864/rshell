#include <iostream>
#include <string>

int main()
{
	//taking in the command from the terminal
	std::cout << "$ ";
	std::string command;
	getline(std::cin, command);
	//parsing through the command
	return 0;
}
